#include <iostream>
int main() {
    bool b = true;
    int x = 0;
    //This replaces if condition is b is true x will 1 and if b is flase x will be 0
    x = (b) ? 1 : 0;
    std::cout << x << std::endl;

}